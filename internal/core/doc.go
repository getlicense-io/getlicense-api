// Package core defines the system language used across every layer:
// typed IDs (AccountID, ProductID, LicenseID, ...), domain enums
// (LicenseStatus, Environment, ...), the AppError type plus error
// codes, the Cursor + Page[T] pagination shape, and ActorKind. The
// package has zero dependencies on other internal/ packages.
package core
