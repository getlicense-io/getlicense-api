// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: refresh_tokens.sql

package sqlcgen

import (
	"context"
	"time"

	"github.com/jackc/pgx/v5/pgtype"
)

const consumeRefreshToken = `-- name: ConsumeRefreshToken :one
DELETE FROM refresh_tokens
WHERE token_hash = $1
  AND expires_at > NOW()
RETURNING identity_id
`

// Atomically remove a refresh token row, returning identity_id when
// the token existed AND was unexpired. Returns ErrNoRows when the
// token was already consumed, expired, or never existed.
//
// Used by auth.Service.Refresh to close the rotation race: two
// concurrent refresh requests with the same token race on this DELETE,
// and only one gets the identity_id back. The other returns ErrNoRows
// and the service rejects with ErrAuthenticationRequired.
func (q *Queries) ConsumeRefreshToken(ctx context.Context, db DBTX, tokenHash string) (pgtype.UUID, error) {
	row := db.QueryRow(ctx, consumeRefreshToken, tokenHash)
	var identity_id pgtype.UUID
	err := row.Scan(&identity_id)
	return identity_id, err
}

const createRefreshToken = `-- name: CreateRefreshToken :exec
INSERT INTO refresh_tokens (id, identity_id, token_hash, expires_at)
VALUES ($1, $2, $3, $4)
`

type CreateRefreshTokenParams struct {
	ID         pgtype.UUID
	IdentityID pgtype.UUID
	TokenHash  string
	ExpiresAt  time.Time
}

func (q *Queries) CreateRefreshToken(ctx context.Context, db DBTX, arg CreateRefreshTokenParams) error {
	_, err := db.Exec(ctx, createRefreshToken,
		arg.ID,
		arg.IdentityID,
		arg.TokenHash,
		arg.ExpiresAt,
	)
	return err
}

const deleteRefreshTokenByHash = `-- name: DeleteRefreshTokenByHash :exec
DELETE FROM refresh_tokens WHERE token_hash = $1
`

func (q *Queries) DeleteRefreshTokenByHash(ctx context.Context, db DBTX, tokenHash string) error {
	_, err := db.Exec(ctx, deleteRefreshTokenByHash, tokenHash)
	return err
}

const deleteRefreshTokensByIdentity = `-- name: DeleteRefreshTokensByIdentity :exec
DELETE FROM refresh_tokens WHERE identity_id = $1
`

func (q *Queries) DeleteRefreshTokensByIdentity(ctx context.Context, db DBTX, identityID pgtype.UUID) error {
	_, err := db.Exec(ctx, deleteRefreshTokensByIdentity, identityID)
	return err
}

const getRefreshTokenByHash = `-- name: GetRefreshTokenByHash :one
SELECT id, identity_id, token_hash, expires_at, created_at
FROM refresh_tokens WHERE token_hash = $1
`

func (q *Queries) GetRefreshTokenByHash(ctx context.Context, db DBTX, tokenHash string) (RefreshToken, error) {
	row := db.QueryRow(ctx, getRefreshTokenByHash, tokenHash)
	var i RefreshToken
	err := row.Scan(
		&i.ID,
		&i.IdentityID,
		&i.TokenHash,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}
