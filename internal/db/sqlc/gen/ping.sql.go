// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: ping.sql

package sqlcgen

import (
	"context"
)

const ping = `-- name: Ping :one

SELECT 1::int AS ok
`

// Placeholder query. sqlc v1.29.0 errors on a queries/ dir with no
// .sql files, so this bootstraps the generate pipeline before real
// queries land in later tasks. Remove or replace in Task 2+.
func (q *Queries) Ping(ctx context.Context, db DBTX) (int32, error) {
	row := db.QueryRow(ctx, ping)
	var ok int32
	err := row.Scan(&ok)
	return ok, err
}
